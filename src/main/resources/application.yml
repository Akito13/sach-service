spring:
  application:
    name: "sach"
  config:
    import: "configserver:http://localhost:8071"
    name: sach

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
  info:
    env:
      enabled: true
